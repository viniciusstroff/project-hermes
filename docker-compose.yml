networks:
  hermes-net:
    name: hermes-net

services:
  app:
    build:
      context: ./
    container_name: project-hermes
    volumes:
      - ./:/home/project-hermes
    networks:
      - hermes-net
    ports:
      - "5678:5678"
    extra_hosts:
      - "host.docker.internal:host-gateway"
